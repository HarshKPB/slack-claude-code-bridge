# Copy to .env and fill. NEVER commit .env.
SLACK_BOT_TOKEN=xoxb-...
SLACK_APP_TOKEN=xapp-...
# Your Slack member ID. Only this user's messages are processed.
ALLOWED_USER_ID=U...
# Default dir Claude runs in. Override per-message with "cd <path>" prefix.
DEFAULT_PROJECT_DIR=/Users/harshkhandelwal/Desktop
# Optional: model override (blank = Claude Code default)
CLAUDE_MODEL=
